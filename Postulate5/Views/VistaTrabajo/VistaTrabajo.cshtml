<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Trabajos</title>

  
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/Trabajo.css" asp-append-version="true" />

 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- Script personalizado -->
    <script src="~/js/VistaTrabajo.js"></script>
</head>

<body>

       <!-- Toast para mostrar mensajes -->
<div class="toast align-items-center text-bg-primary border-0" id="myToast" role="alert" aria-live="assertive" aria-atomic="true" data-bs-delay="10000" style="position: fixed; top: 280px; right: 20px; z-index: 1050;">
    <div class="d-flex">
        <div class="toast-body">
           Busca la rpfesion especifica con el filtro !
        </div>
        <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
    </div>
</div>
   <h1 class="tit"><i class="fa-solid fa-pencil"></i> Todos los Trabajos <i class="fa-solid fa-hammer"></i></h1>
   <p class="parrafo_servicio"><i class="fa-solid fa-exclamation"></i> En esta sección se encuentran todos los trabajos propuestos y no acordados de los usuarios logueados.</p>

   <!-- Input de búsqueda -->
   <div class="input d-flex justify-content-center">
      <div class="col-md-6 input_Buqueda">
          <label for="ProfesionID" class="form-label titulo_input">Buscar Profesión Interesada</label>
          @Html.DropDownList("profesionBuscarID", ViewBag.ProfesionID as SelectList, htmlAttributes: new {
              @class = "form-control ubicacion_input",
              @onchange = "CardTrabajos()" 
          })
      </div>
   </div>

   <!-- Contenedor principal de las tarjetas -->
   <div class="contenedor-card" id="TrabajoID">
      <div id="contenedorCards" class="row"></div>
   </div>

   <!-- Modal para agregar un nuevo trabajo -->
   <div class="modal fade" id="agregarTrabajo" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
      <div class="modal-dialog">
          <div class="modal-content modal_style">
              <div class="modal-header">
                  <h1 class="modal-title" id="exampleModalLabel" style="color: white;">Nuevo Trabajo</h1>
                  <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
              </div>
              <div class="modal-body">
                  <form method="post" class="row g-3">
                      <input type="hidden" id="TrabajoID">
                      <div class="col-md-6">
                          <label for="PersonaID" class="form-label">Nombre</label>
                          @Html.DropDownList("PersonaID", null, htmlAttributes: new { @class = "form-control" })
                      </div>
                      <div class="col-md-6">
                          <label for="ProfesionID" class="form-label">Profesion</label>
                          @Html.DropDownList("ProfesionID", null, htmlAttributes: new { @class = "form-control" })
                      </div>
                      <div class="col-md-6">
                          <label for="direccion" class="form-label">Direccion</label>
                          <input type="text" id="direccion" class="form-control" placeholder="Dirección">
                      </div>
                      <div class="col-md-6">
                          <label for="descripcion" class="form-label">Descripción</label>
                          <input type="text" id="descripcion" class="form-control" placeholder="Descripción">
                      </div>
                      <div class="col-md-6">
                          <label for="comentario" class="form-label">Comentario</label>
                          <input type="text" id="comentario" class="form-control" placeholder="Comentario">
                      </div>
                      <div class="col-md-6">
                          <label for="hora" class="form-label">Hora solicitada</label>
                          <input type="datetime-local" id="hora" class="form-control" placeholder="Hora">
                      </div>
                      <div class="col-md-6">
                          <label for="fecha" class="form-label">Fecha</label>
                          <input type="date" id="fecha" class="form-control" placeholder="Fecha">
                      </div>
                  </form>
              </div>
              <div class="modal-footer">
                  <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                  <button type="button" class="btn btn-primary" onclick="agregarTrabajo()">Guardar cambios</button>
              </div>
          </div>
      </div>
   </div>

   <!-- Toast para mostrar mensajes -->
   <div class="toast align-items-center text-bg-primary border-0" id="myToast" role="alert" aria-live="assertive" aria-atomic="true" data-bs-delay="10000" style="position: relative; z-index: 1050;">
      <div class="d-flex">
         <div class="toast-body">
            Atención: Mientras más datos ingreses, mejor para tu perfil de trabajos.
         </div>
         <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
      </div>
   </div>

   <script>
      // Mostrar el toast cuando se cargue la página
      document.addEventListener('DOMContentLoaded', function () {
          var myToast = new bootstrap.Toast(document.getElementById('myToast'));
          myToast.show();
      });
   </script>

   @section Scripts {
      <script src="~/js/VistaTrabajo.js"></script>
   }
</body>

</html>
